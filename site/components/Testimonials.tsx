import type { Dictionary } from "@/content/dictionary";

export default function Testimonials({ dict }: { dict: Dictionary }) {
  // Testimonials have been removed - this component is no longer used
  return null;
}
